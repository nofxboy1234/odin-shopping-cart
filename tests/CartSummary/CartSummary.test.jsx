import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import StyledCartSummary from '../../src/components/CartSummary/CartSummary';

describe.skip('CartItem component', () => {
  it('renders the title, total cart quantity, total cart price, checkout button', () => {});

  describe('when clicking the checkout button', () => {
    it('shows a thanks alert', () => {});
  });
});
